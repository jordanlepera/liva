// /** @type {import('next-sitemap').IConfig} */
// module.exports = {
//   siteUrl: 'https://livamaconnerie.netlify.app/', // FIXME: Change to the production URL
//   generateRobotsTxt: true,
// };

const nextSitemapConfig = {
  siteUrl: 'https://livamaconnerie.netlify.app/', // FIXME: Change to the production URL
  generateRobotsTxt: true,
};

export default nextSitemapConfig;
